version: 2

jobs:
  build:
    docker:
      - image: circleci/php:7.1

    steps:
      - checkout

      # Dependencies
      - run: sudo apt install php7.1-gd

      - run: make lint-venv

      # Config
      - run: make config

      # Tests
      - run: ./run-tests --full-output
